// Package style provides functions that decorate text (like formatting,
// changing case or colors) using several idioms (plain text, (non-)colored
// term,  mandoc, markdown). Proposed styles are supposed to be easily
// extendable.
package style
